import "Hud.fcc" as Hud
import "StdLibrary.fcc" as StdLib
import "EditorGenLib.fcc" as EditorLib

graph UIWidgetButtonUtils {
    event OnTapped(player entity<Player>) {
        DispatchEventWithPlatform(OnTapButton, thisEntity<UIWidget>.ParentUI, PlatformType.Local, List<object>{player, thisEntity})
    }
}